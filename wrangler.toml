# pilot-consumer Workers 部署配置（试点走 Cloudflare，不走 ECS）
name = "pilot-consumer"
main = "backend/worker.js"
compatibility_date = "2026-09-19"
workers_dev = false

# 自定义域名直连（免 workers.dev 开通）：zone 内路由
[[routes]]
pattern = "pilot-api.linkseek.net.cn/*"
zone_name = "linkseek.net.cn"
